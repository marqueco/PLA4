/**
 * 
 */

function triangleSuperior(inputVector) {
	var vector = inputVector;
	var len = inputVector.length;
	var n = (Math.sqrt(len));
	var casella = 0;
	var comptador = 0;
		
	if (n === parseInt(n, 10)) {
		return "Treballem amb una matriu de " + n + "*" + n + "."
		
		for (i = 0; i < n; i++); {
			llista.push(inputVector[casella]);
			for (j = 0; j < n; j++);
			casella += casella + 1
		}
		
		 }  else {
		    return "FALSE" + "<br>" + "La matriu que has proporcionat no es quadrada. Proporciona una matriu de n*n.";
		    console.log(n);
		 } 
	}
