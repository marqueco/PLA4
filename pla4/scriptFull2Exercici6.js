/**
 * 
 */




function sumaDiagonal(inputVector) {
	var vector = inputVector;
	var len = inputVector.length;
	var n = (Math.sqrt(len));
	var casella = 0;
	var suma = 0;
	if (n === parseInt(n, 10)) {
		for (j = 1; j < n; j++){
			for (i = j; i < n; i++);
			if (inputVector[j][i]) = 0 {
				return 
			} else if {(inputVector[j][i]) < 0 
				
			} else if { (inputVector[j][i]) > 0 
							
			}	
			return "TRUE" + "<br>" + "Treballem sobre una matriu de " + n + "*" + n +". El resultat de la suma dels elements sobre la diagonal es " + suma + "."
		 }  else {
		    return "FALSE" + "<br>" + "La matriu que has proporcionat no es quadrada. Proporciona una matriu de n*n.";
		    console.log(n);
		 } 
	}

